{
  "name": "http-x-inspector",
  "version": "1.1.0",
  "description": "HTTP X-Inspector is a diagnostic tool that captures and displays detailed information of raw HTTP requests, enabling developers to quickly identify and resolve issues with headers, methods, payloads, and more.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "devDependencies": {
    "@types/node": "^20.10.4",
    "typescript": "^5"
  }
}
